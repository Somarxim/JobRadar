# JobRadar 开发路线图

| 版本 | 日期 | 状态 |
|---|---|---|
| v0.27 | 2026-09-13 | LLM 语义分类 fallback：规则分类器返回 OTHER 时，调轻量 LLM 识别公司名所属类型（如亿通国际→internet），结果内存缓存避免重复调用；LLM 未配置/失败时降级 OTHER 不阻断爬虫。三层架构：规则层（快+零成本）→ LLM fallback 层（泛化补盲）→ 缓存层（去重） |
| v0.26 | 2026-09-13 | 投递管理页加列表视图：BoardPage 顶部「看板/列表」toggle（localStorage 记忆），表格展示公司·岗位/城市/阶段/优先级/待办/最近更新，支持搜索+阶段筛选。看板适合做流转操作，列表适合做信息检索，两者互补 |
| v0.25 | 2026-09-13 | 简历删除/归档：resumes 表加 archived 软删标记，list 过滤归档项；删除默认简历时自动转移默认资格；前端卡片加删除按钮 + 确认弹窗说明历史数据保留。解决秋招多版本简历堆积误选问题 |
| v0.24 | 2026-09-11 | 手动爬取入口：今日推荐卡片加「立即爬取」按钮 + 「上次爬取」时间戳（未爬当日琥珀色提醒），爬完 toast 汇总抓到/新入库数并引导立即推荐；纯前端补 UI（crawl 端点 W3-1 已有）。本地部署下定时窗不可靠，手动点按成为数据新鲜度的 baseline |
| v0.23 | 2026-09-11 | 每日推荐候选兜底：36h 无新岗时放宽到近 30 天活跃未推荐岗位（看板排除与 7 天防重不变），修复「爬虫断档日推荐静默为空」；根因=定时任务错过窗口不追补 + 候选窗口过紧 |
| v0.22 | 2026-09-11 | 企业性质自动识别（规则分类器：运营商/银行/研究所/外企/互联网/国企有序词典，创建路径全覆盖 + 存量 OTHER 启动回填 + PATCH 手动纠正口）+ 岗位库同公司折叠分组（≥2 岗出组头，默认开，localStorage 记忆）。公司类型筛选从空转（165/168 全 OTHER）变为可用 |
| v0.21 | 2026-09-11 | 路由级错误边界：errorElement 兜底渲染崩溃（含 404 分支），替代 react-router 默认白屏堆栈页；Dashboard 崩溃根因定位为 vite 预打包缓存过期（非代码 bug） |
| v0.20 | 2026-09-10 | 细节修缮③：公司分级前端入口（PATCH /companies/{id} + 详情页内联编辑 + 列表筛选/徽章）+ 看板卡片待办编辑弹窗 + 前端拆包（路由 lazy + rolldown advancedChunks，500KB 警告消除）。W3-4 投递规划三要素齐（分级/周目标/计划vs实际） |
| v0.19 | 2026-09-10 | 细节修缮②：全文检索上线——jieba 应用层分词写 search_text（录入/导入/爬虫/更新四路径覆盖 + 启动回填存量），搜索从 LIKE 切 tsvector+tsquery（GIN 索引）；V2 预留的 FTS 基建正式启用 |
| v0.18 | 2026-09-10 | 细节修缮①：智联复测——kw 被静默忽略（软反爬），匿名搜索已失效，保持禁用并将结论随 V7 落库；后续走 Playwright/插件路线 |
| v0.17 | 2026-09-10 | W4-4 完成：全页统一加载/错误/空态（StatusStates + 重试入口）、README 快速开始补全、learning 新增 mcp-protocol.md 与 interview-guide.md（STAR 话术 + Q&A 树 + 演示动线）。W4 开发项全部交付，验收待用户联调 |
| v0.16 | 2026-09-10 | W4-3 完成：Dashboard 图表区（漏斗/30天趋势/公司类型分布，recharts）+ 每周复盘卡片（W4-2 前端出口，AI 叙事按需生成） |
| v0.15 | 2026-09-10 | W4-2 完成：周报 Agent（确定性数据组装 + LLM 叙事降级分层；REST /weekly-report + MCP weekly_report 支持历史周回看） |
| v0.14 | 2026-09-10 | W4-1 完成：MCP Server 落地（10 tools + 2 resources + 3 prompts，stdio 协议冒烟通过，含 commons-logging 排雷与 LLM/简历装配下沉 core） |
| v0.13 | 2026-09-10 | W3 收尾：待办自动生成（流转驱动）+ 爬虫 QA 成稿 + 数据源探测（智联匿名可用已落地解析器默认禁用；51job/应届生被阿里云 WAF 搁置）。W3 主链路闭环，W3-4/W3-5 转入细节修缮清单，优先 W4 |
| v0.11 | 2026-09-09 | W3-2 完成：牛客校招 JSON API 接通（4 关键词源真实产出，首轮 162 岗位）；国聘网实测需登录态暂缓 |
| v0.12 | 2026-09-09 | W3-3 完成：每日推荐管线 + Dashboard 今日推荐区 + 反馈闭环；MatchingService 改 REQUIRES_NEW 修事务污染隐患 |
| v0.10 | 2026-09-09 | W3-1 完成：爬虫框架（调度/抓取/解析/清洗/去重管线 + 源插件化 + 失败隔离） |
| v0.9 | 2026-09-08 | W2 验收 UX 修正：文本解析改 AI 补全职责、海报两段式、插件三层提取抗噪音 |
| v0.8 | 2026-09-05 | W2 收尾：LLM 每日成本闸 + Testcontainers 集成测试基线（W2 全部完成） |
| v0.7 | 2026-09-05 | W2-5 完成：Chrome 插件 v1（采集入口闭环） |
| v0.6 | 2026-09-05 | W2-4 完成：匹配 Agent v1（LLM 精评 + 报告落库 + 详情页展示） |
| v0.5 | 2026-09-05 | W2-3 完成：简历上传 + PDFBox 解析 + ResumeProfile 结构化档案（匹配 Agent 数据前置） |
| v0.4 | 2026-09-05 | W2-1 完成：LLM 接入（DeepSeek 解析 + qwen-vl 视觉路由）+ token 记账 + JD 免 hints 导入 |
| v0.3 | 2026-09-04 | W2 新增海报图片多模态导入（用户反馈：研究所公众号招聘多为海报大图） |
| v0.2 | 2026-09-04 | 技术栈切换 Java/Spring（ADR-0），任务描述同步 |
| v0.1 | 2026-09-03 | 初稿 |

起点：2026-09-03（秋招进行中）。原则：**每周都有可用交付，Week 1 结束即开始真实使用**，后续边用边迭代。

## 1. 四周计划

### Week 1（09.03–09.09）：MVP —— 先把记录管起来

**目标**：替代手工备忘录，所有投递进系统。

- [ ] 项目骨架：Maven 多模块（core/app/mcp-server）、docker-compose 起 pgvector、Flyway 首次迁移、Spring Boot 启动
- [ ] 数据模型落地：JPA Entity + Flyway V1__init.sql（含 tsvector 生成列、pgvector 索引）
- [ ] API：jobs CRUD + 手动录入/粘贴导入（先纯文本，LLM 解析 W2 接入）+ applications 流转 + events
- [ ] API：dashboard/summary + dashboard/calendar
- [ ] 前端骨架：Vite + Tailwind + shadcn/ui + 路由 + API client
- [ ] 前端页面：岗位库（表格+搜索筛选）、看板（拖拽流转）、岗位详情、日历
- [ ] 基础 Dashboard：漏斗数字卡 + DDL 倒计时列表

**验收**：手动录入 10 个真实岗位；完成一次「收藏→计划→投递→笔试」全流程流转；日历正确显示 DDL；`pytest` 核心流转用例通过。

### Week 2（09.10–09.16）：采集与匹配 —— AI 能力进场

- [x] Spring AI 多模型接入（DeepSeek 文本解析 + DashScope qwen-vl 视觉，OpenAI 兼容协议路由）+ token 记账（llm_usage 表）
- [x] JD 文本导入接 LLM 结构化（ingest hints 可选，手填优先 AI 补全；JobRequirements 结构化并入匹配 Agent 切片）
- [x] **海报图片导入**：ingest 扩展 image_base64 输入，qwen-vl-plus 多模态直读（版式+文字+JD 整理一体），失败降级 422 人工兜底。已实现并联调通至模型端点；当前 DashScope 免费额度耗尽，待充值/换模型后可用（2026-09-05 实测 403 FreeTierOnly，降级与失败记账路径已验证）
- [x] 简历上传 + PDFBox 解析 + ResumeProfile 确认页（上传/列表/详情/reparse/默认切换 + AI 档案确认弹窗；字段级人工编辑留待匹配联调时补）
- [x] 匹配 Agent v1（LLM 精评 + 24h 缓存 + 服务端规则兜底；pgvector 粗筛随 W3 规模场景上线）+ 岗位详情页匹配报告展示
- [x] Chrome 插件 v1：popup 表单 + 页面提取（选中优先）+ 令牌推送本地 API（真实浏览器三站点实测待用户操作）
- [x] Testcontainers 集成测试基线（4 用例覆盖幂等/流转事件/归档过滤/简历降级；含 Docker 29 API 兼容修复）

**验收**：插件在 BOSS/牛客/某国企官网各收藏 1 个真实岗位成功入库；上传真实简历完成解析；对 5 个岗位出匹配报告，人工判断结论合理。

### Week 3（09.17–09.23）：发现引擎 —— 解决信息盲区

- [x] 爬虫框架（调度/抓取(Jsoup)/解析/清洗/去重管线；站点适配接 W3-2）
- [x] 站点适配按 [target-sources.md](target-sources.md) §7 顺序：~~国聘网~~（实测需登录态，暂缓）+ 牛客校招（已上线：JSON API 匿名可用，4 关键词源日产出）；~~智联~~（解析器已落地，无校招过滤噪音大默认禁用）~~51job/应届生~~（阿里云 WAF 拦截搁置）；航空工业/航天系、运营商银行官网转细节修缮
- [x] 每日推荐管线（@Scheduled 07:45）+ Dashboard 今日推荐区 + 反馈闭环（accept 自动进看板；LLM 不可用降级规则粗排；真实 E2E：173 候选→Top5）
- [x] 待办事项自动生成（设计修改：流转驱动默认待办 + 自定义文案保护 + 终态清空）
- [ ] ~~投递规划：公司分级 + 周目标 + 计划 vs 实际视图~~（转细节修缮清单）
- [ ] ~~语义搜索上线~~（转细节修缮清单）

**验收**：连续 3 天每日推荐有产出（跑到 09-12 自然观察）；至少 1 条推荐是"之前不知道的机会"；~~周目标完成度可视化~~（随投递规划后补）。

### 细节修缮清单（主体完成后回填）

- ~~W3-4 投递规划~~（2026-09-10 完成：公司分级入口 feat/tier-todo-ui；周目标 + 计划vs实际在 W4-2 周报卡片）
- W3-5 语义搜索（pgvector 粗筛上线）
- 站点适配：~~智联关键词调优启用~~（2026-09-10 复测：kw 被静默忽略，软反爬，调优无意义，详见 V7 + document/plans/2026-09-10-zhilian-reprobe.md；后续 Playwright/插件路线）、国聘（插件路线带登录态）、航空工业/航天系/运营商银行官网
- ~~看板卡片"编辑待办"入口~~（2026-09-10 完成：卡片悬停铅笔按钮 + 编辑弹窗，feat/tier-todo-ui）
- 51job/应届生如需要：Playwright 过 WAF 或插件路线

### Week 4（09.24–09.30）：MCP 与打磨 —— 简历亮点封装

- [x] MCP Server：Spring AI MCP Starter，10 个 tools + resources + prep_interview prompt（stdio 协议级冒烟通过；Claude Desktop 联调待用户在客户端配置后重启）
- [x] 周报 Agent（确定性数据 + LLM 叙事分层，REST/MCP 双出口，支持历史周回看）
- [x] Dashboard 图表完善（漏斗图/趋势图/类型分布 + 每周复盘卡片）
- [x] 打磨：加载态/空态/错误处理（统一 StatusStates 组件 + 全页重试入口）；README 完善；docs/learning/interview-guide.md + mcp-protocol.md 成稿（演示录屏待用户按 §2 脚本录制）

**验收**：Claude Desktop 内完成「录入-投递-查询-复盘」全对话流程（开发就绪，待用户配置 claude_desktop_config.json 并联调）；录制 3 分钟演示视频（待用户操作）；简历描述定稿。

## 2. 里程碑与演示脚本

| 里程碑 | 时间 | 演示点 |
|---|---|---|
| M1 能用 | W1 末 | 看板流转 + 日历 |
| M2 聪明 | W2 末 | 插件收藏 + 简历匹配报告 |
| M3 发现 | W3 末 | 每日推荐命中盲区岗位 |
| M4 亮点 | W4 末 | Claude Desktop 自然语言管理全流程 |

**M4 演示脚本（简历面试用）**：
1. 打开 Dashboard 展示漏斗与日历 → 「这是我的秋招全景」
2. 插件收藏一个新岗位 → 「多平台信息统一归集」
3. 展示该岗位匹配报告（硬性条件核对）→ 「AI 帮我做投递决策」
4. Claude Desktop 对话：「我这周进度如何」→「把刚收藏的岗位标记为已投递」→「生成面试准备」→ 「数据库由我封装成了 MCP 工具」

## 3. 风险登记

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| Java/Spring AI 熟练度不足拖慢开发 | 中 | W1–W2 进度 | AI 辅助全栈实现 + 遵循 Spring 官方指南；同时视为银行/国企 Java 面试的实战备战，面试话术同步积累 |
| Spring AI / MCP Starter 版本变动（生态较新） | 中 | 个别 API 不兼容 | 锁定初始化时稳定版本；遇 breaking change 以官方迁移指南为准并记 ADR |
| 爬虫目标站改版/反爬升级 | 中 | 某源失效 | 源插件化配置，单源失败隔离；先跑通国聘+牛客即可 |
| LLM 结构化输出不稳定 | 中 | 解析失败 | schema 校验+重试+降级人工录入（422 流程） |
| 秋招事务挤占开发时间 | 高 | 进度延后 | W1 后已可用；后续周目标可砍半，P2 功能让位 |
| LLM 调用成本超预期 | 低 | 费用 | 成本闸（每日限额）+ 粗筛先行 |
| Chrome 插件提取规则不适配某站 | 中 | 收藏信息不全 | 提取失败降级为「原始文本粘贴」+ LLM 清洗兜底 |
| 简历隐私顾虑 | 低 | — | 本地存储 + 脱敏开关（发送 LLM 前隐去姓名电话） |

## 4. 后续迭代池（秋招后）

- 多用户化 + 部署（如果同学想用）
- 面试复盘库（面经结构化沉淀，与岗位关联）
- offer 对比决策 Agent（薪资/城市/发展多维权衡）
- 移动端查看（PWA）
